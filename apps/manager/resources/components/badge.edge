@if(href)
  <a
    href="{{ href }}"
    class="bg-blue-100 text-blue-500 px-3 py-0.5 rounded-full"
  >
    {{ value }}
  </a>
@else
  <div class="bg-blue-100 text-blue-500 px-3 py-0.5 rounded-full">
    {{ value }}
  </div>
@end
